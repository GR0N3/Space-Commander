using UnityEngine;

// Minimal Drone component so DronePool and other scripts can reference the type.
// Add functionality to this class as needed (movement, shooting, lifecycle, etc.).
public class Drone : MonoBehaviour
{
    // Placeholder: expand with behaviour later.
}
